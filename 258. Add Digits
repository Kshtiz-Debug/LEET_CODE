Given an integer num, repeatedly add all its digits until the result has only one digit, and return it.



code:


class Solution:
    def addDigits(self, num: int) -> int:
        self.num=num
        if num==0:
            return 0
        return 1+(num-1)%9

-------------------------------------------------------------------------------------------------------------------------------

or you can use the basics 

code:


class Solution:
    def addDigits(self, num: int) -> int:
        self.num=num
        sum1=0
        while(len(str(num))!=1):
            for i in str(num):
                sum1=sum1+int(i)
            num=sum1
            sum1=0
        return num
